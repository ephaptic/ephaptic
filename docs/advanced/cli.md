The CLI is accessed by running `ephaptic` in your terminal.

If it doesn't work, you may need to run `.venv/bin/ephaptic` or `~/.local/bin/ephaptic`. Consider adding these to PATH.

::: mkdocs-click
    :module: ephaptic.cli.__main__
    :command: click
    :prog_name: ephaptic